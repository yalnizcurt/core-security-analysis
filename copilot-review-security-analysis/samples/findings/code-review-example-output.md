# Code Review — Example Output

> This is the expected output when the Code Review Agent analyzes `sample-app/server.js` and `sample-app/utils.js`. Use this to verify your agent is producing similar results.

---

## 🟠 Code Review — `sample-app/server.js`

**Severity:** HIGH
**Findings:** 6

### Details

1. **Line 10:** Hardcoded API key (`API_KEY = "sk-proj-..."`) — move to environment variables
2. **Line 11:** Hardcoded database password (`DB_PASSWORD = "supersecret123"`) — use a secrets manager
3. **Line 55:** SQL query built with string interpolation — use parameterized queries
4. **Line 73:** `eval()` used on user input — replace with a safe JSON parser
5. **Line 89:** MD5 used for password hashing — use bcrypt or Argon2
6. **Line 105:** Unescaped user input rendered in HTML — sanitize output

### What Went Well

- Express server structure follows standard patterns
- Routes are logically organized
- Error responses include meaningful messages

---

## 🟡 Code Review — `sample-app/utils.js`

**Severity:** MEDIUM
**Findings:** 5

### Details

1. **Lines 15–38:** Deeply nested conditionals (4+ levels) in `processUserData()` — extract to helper functions
2. **Lines 55–68:** Magic numbers (0.1, 0.15, 0.2, 100, 500, 1000) in `calculateDiscount()` — define as named constants
3. **Lines 72, 85, 91:** `console.log("DEBUG: ...")` statements left in production code — remove or use a logger
4. **Lines 93–118:** Duplicated validation logic in `validateEmail()` and `validatePhone()` — extract shared validation pattern
5. **Lines 120–145:** Callback hell in `processFiles()` — refactor to async/await

### What Went Well

- Functions are named descriptively
- Logic captures real business rules
- Test file exists with basic coverage

---

*Generated by Copilot SDK Code Review Agent*
*This is sample output for workshop verification. Actual line numbers may vary.*
